namespace PrimeNumbers
{
    using System;

    class Program
    {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());
            bool[] A = new bool[n];

            for (int i = 2; i < n; i++)
            {
                A[i] = true;   
            }

            for (int i = 0; i < ; i++)
            {

            }
        }
    }
}
